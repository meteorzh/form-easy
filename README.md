# form-easy

基于 Stencil 的动态表单组件库，以及 Vue 集成与示例应用。

## 包

- `form-easy`：Stencil 核心 Web Components。
- `form-easy-vue`：Vue 项目中的封装、继承和类型支持。
- `form-easy-vue-playground`：本地示例应用，不发布到 npm。

## 开发

```bash
npm install
npm run dev
```

执行 `npm run build` 构建所有可构建的工作区包。
